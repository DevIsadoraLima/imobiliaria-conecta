function tenhoInteresse(params) {
    const input = prompt("Informe seu seu nome e email para contato:")
    if (input != null) {
        alert("Interesse registrado com sucesso! Entraremos em contato em breve.")
    }
}